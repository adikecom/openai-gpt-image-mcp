# 🚀 Настройка OpenAI GPT Image MCP для Cursor

## ✅ Установка завершена

Проект успешно установлен и собран:
- ✅ Зависимости установлены
- ✅ Проект собран (`dist/index.js`)

---

## 🔧 Настройка для Cursor

### Шаг 1: Найдите конфигурационный файл Cursor

Конфигурация MCP для Cursor обычно находится в одном из следующих мест:

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/rooveterinaryinc.roo-cline/settings/cline_mcp_settings.json
```

или

```
~/.cursor/mcp.json
```

**Linux:**
```
~/.config/Cursor/User/globalStorage/rooveterinaryinc.roo-cline/settings/cline_mcp_settings.json
```

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\rooveterinaryinc.roo-cline\settings\cline_mcp_settings.json
```

### Шаг 2: Добавьте конфигурацию

Откройте файл конфигурации и добавьте следующую конфигурацию:

```json
{
  "mcpServers": {
    "openai-gpt-image-mcp": {
      "command": "node",
      "args": [
        "/Users/adiksm86icloud.com/isistant-landing/openai-gpt-image-mcp/dist/index.js"
      ],
      "env": {
        "OPENAI_API_KEY": "sk-your-api-key-here"
      }
    }
  }
}
```

**Важно:** Замените:
- Путь на **абсолютный путь** к `dist/index.js`
- `sk-your-api-key-here` на ваш реальный OpenAI API ключ

### Шаг 3: Получите OpenAI API ключ

1. Перейдите на https://platform.openai.com/api-keys
2. Создайте новый API ключ
3. **Важно:** Убедитесь, что у вас есть доступ к Image API (требуется верифицированная организация OpenAI)

### Шаг 4: Перезапустите Cursor

После добавления конфигурации перезапустите Cursor, чтобы изменения вступили в силу.

---

## 🔐 Альтернатива: Использование .env файла

Если вы не хотите хранить API ключ в конфигурации, можете использовать .env файл:

1. Создайте файл `.env` в директории проекта:
```bash
cd /Users/adiksm86icloud.com/isistant-landing/openai-gpt-image-mcp
echo "OPENAI_API_KEY=sk-your-api-key-here" > .env
```

2. Обновите конфигурацию Cursor:
```json
{
  "mcpServers": {
    "openai-gpt-image-mcp": {
      "command": "node",
      "args": [
        "/Users/adiksm86icloud.com/isistant-landing/openai-gpt-image-mcp/dist/index.js",
        "--env-file",
        "/Users/adiksm86icloud.com/isistant-landing/openai-gpt-image-mcp/.env"
      ]
    }
  }
}
```

---

## 🧪 Проверка установки

После настройки вы сможете использовать следующие инструменты в Cursor:

1. **create-image** - Генерация изображений из текстового промпта
2. **edit-image** - Редактирование изображений (inpainting, outpainting)

---

## ⚠️ Важные замечания

1. **Верифицированная организация OpenAI:** Для доступа к Image API требуется верифицированная организация. После верификации может потребоваться 15-20 минут для активации доступа.

2. **Абсолютные пути:** В конфигурации должны использоваться абсолютные пути (начинающиеся с `/` на macOS/Linux).

3. **Размер файлов:** MCP имеет лимит 1MB для ответов. Большие изображения автоматически сохраняются на диск.

4. **Рабочая директория:** Для больших файлов можно установить переменную окружения:
   ```json
   "env": {
     "OPENAI_API_KEY": "sk-...",
     "MCP_HF_WORK_DIR": "/path/to/your/directory"
   }
   ```

---

## 🐛 Устранение неполадок

### Проблема: "API key is invalid"
- Проверьте, что API ключ правильный
- Убедитесь, что у вас есть доступ к Image API

### Проблема: "File not found"
- Проверьте, что путь к `dist/index.js` абсолютный
- Убедитесь, что файл существует: `ls -la /Users/adiksm86icloud.com/isistant-landing/openai-gpt-image-mcp/dist/index.js`

### Проблема: "No access to image API"
- Убедитесь, что у вас верифицированная организация OpenAI
- Подождите 15-20 минут после верификации

---

## 📚 Дополнительная информация

- [OpenAI Images API Documentation](https://platform.openai.com/docs/api-reference/images)
- [MCP SDK Documentation](https://modelcontextprotocol.io/)

---

## ✅ Готово!

После настройки вы сможете использовать генерацию и редактирование изображений прямо в Cursor через MCP протокол.

